#pragma once
#include <iostream>
#include <vector>
#include <string>
using namespace std;

class Player
{
public:
	Player();
	vector<vector<string>> Player::playerTurn(vector<vector<string>> board);
};